function Logout() {
    // const handleLogoutClick() {
    //     fetch("/api/logout", { method: "DELETE" }).then((r) => {
    //       if (r.ok) {
    //         setUser(null);
    //       }
    //     });
    //   }
};

export default Logout