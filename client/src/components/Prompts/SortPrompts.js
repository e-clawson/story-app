// const sortPrompts = () => {
//     const sortedPrompts = [...filteredPromptd].sort(function(a, b) {
//         var nameA = a.name[].toUpperCase();
//         var nameB = b.name[].toUpperCase(); 
//         if (nameA < nameB) {
//           return -1;
//         }
//         if (nameA > nameB) {
//           return 1;
//         }
//         return 0;
//     })
//     setFilteredPrompts(sortedPrompts)
// }



// return(
//     <div>
//         <PromptFilter handleSearch={handleSearch}/>
//         <button onClick={() => setFavoriteView(bool => !bool)}> Favorites 💖</button>
//         <button onClick={() => sortedPrompts()}> Sort Alphabetically </button>
//         <PromptList prompts={favoriteView ? favoritePrompts : filteredPrompts} addToFavorites={addToFavorites}/>
//     </div> 
// )
// }
